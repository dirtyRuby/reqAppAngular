# Directive to view value

angular.module('reqApp')
.directive('viewValue', ->
  restrict: 'E',
  templateUrl: "<%= asset_path('view-value.html') %>",
  controller: 'RequestController',
  controllerAs: 'RequestCtrl'
)